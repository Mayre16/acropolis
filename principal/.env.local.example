# CMS local — conecta principal :3100 al API :3401
NEXT_PUBLIC_CMS_URL=http://localhost:3401

# Cloudflare Turnstile (anti-bots en formularios). En desarrollo se usan claves de prueba si no se definen.
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=...
# TURNSTILE_SECRET_KEY=...  # solo en el servidor CMS (editor), no en Next.js
